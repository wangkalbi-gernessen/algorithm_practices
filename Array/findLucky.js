'use  strict';

/**
 * @param {number[]} arr
 * @return {number}
 */
const findLucky = function(arr) {
  let stact = [];
  let count = 1;
  for(let i = 0; i <= arr.length - 1; i++){
    
  }
  
  
  
  
};

console.log(findLucky());